{
  "name": "nutrition-tracker",
  "version": "1.6.0",
  "private": true,
  "description": "Tracker nutritionnel - application web vanilla JS (audit + tests unitaires)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "serve": "node tools/nutrition-app-server.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
